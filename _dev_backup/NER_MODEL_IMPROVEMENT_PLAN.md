# NER 모델 품질 개선 계획

## 현재 상황

| 항목 | 현재 값 | 문제점 |
|------|---------|--------|
| 베이스 모델 | `en_core_web_sm` | 작은 모델, 제한된 word vectors |
| 학습 횟수 | 10 epochs | 부족할 수 있음 |
| 라벨 | DATE, TIME, LOC, EVENT (4개) | 6개로 확장 필요 |
| Dropout | 0.5 | 적절함 |
| 데이터 형식 | `(text, annotations)` 튜플 | 현재 JSON 형식과 불일치 |

---

## 개선 방안

### 1. 🏷️ 라벨 업데이트 (필수)
```python
# 기존: ["DATE", "TIME", "LOC", "EVENT"]
# 변경: 6개 라벨
labels = ["START_DATE", "START_TIME", "END_DATE", "END_TIME", "LOC", "EVENT_TITLE"]
```

### 2. 📊 데이터 로딩 함수 추가 (필수)
현재 JSON 형식 (`Date_Entity`, `End_Date_Entity` 등)을 spaCy 형식으로 변환하는 함수 필요

### 3. 🧠 베이스 모델 선택
| 옵션 | 용량 | 장점 | 단점 |
|------|------|------|------|
| `en_core_web_sm` | 12MB | 빠름 | 정확도 낮음 |
| `en_core_web_md` | 40MB | 균형 | - |
| **`en_core_web_lg`** | 560MB | **word vectors 포함, 정확도 높음** | 느림 |

> **권장**: `en_core_web_lg` (word vectors가 NER 성능에 큰 영향)

### 4. 📈 학습 파라미터 최적화
```python
N_ITER = 30          # 10 → 30 (더 많은 학습)
BATCH_SIZE = (4, 32) # 유지
DROPOUT = 0.35       # 0.5 → 0.35 (과적합 방지 + 학습 효율)
```

### 5. 🔀 Train/Dev 분리 (중요)
```python
# 80% 학습, 20% 검증
train_data = data[:int(len(data) * 0.8)]
dev_data = data[int(len(data) * 0.8):]
```
검증 데이터로 overfitting 모니터링 가능

### 6. 📊 평가 메트릭 추가
학습 중 Precision, Recall, F1-score 출력

---

## 구현 계획

### [MODIFY] [train.py](file:///c:/Users/82105/PycharmProjects/Acdt_Project_Render_ver/train.py)

1. **INPUT_FILE** → `final_training_data.json`
2. **OUTPUT_DIR** → `./output/new_ner_model`
3. **라벨** → 6개로 확장
4. **데이터 로딩** → JSON dict 파싱 함수 추가
5. **베이스 모델** → `en_core_web_lg` (선택적)
6. **N_ITER** → 30
7. **Train/Dev 분리** 추가
8. **평가 로직** 추가

---

## 결정 필요 사항

1. **베이스 모델 선택**: `en_core_web_lg` 사용할까요? (다운로드 필요, ~560MB)
2. **학습 횟수**: 30 epochs 괜찮을까요?
3. **출력 디렉토리**: `./output/new_ner_model` 경로 괜찮을까요?

---

## 예상 결과

| 항목 | Before | After |
|------|--------|-------|
| 라벨 수 | 4개 | **6개** |
| 학습 데이터 | ~500개 | **10,148개** |
| 베이스 모델 | sm | **lg** |
| 학습 횟수 | 10 | **30** |
| 검증 | 없음 | **Train/Dev 분리** |
