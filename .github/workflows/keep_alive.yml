name: Keep Hugging Face Space Alive

on:
  schedule:
    # 매일 6시간마다 실행 (UTC 기준) / Runs every 6 hours
    - cron: "0 */6 * * *"
  workflow_dispatch: # 수동 실행 버튼 추가

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Wake up Hugging Face Space
        run: |
          curl -I https://snowmang-ai-scheduler-g14.hf.space